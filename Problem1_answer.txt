// ConsoleApplication1.cpp : Defines the entry point for the console application.
//
//if there needs to be pch.h included 
//it has to be included before all other includes take place
//includíng allows us to use programms other people already wrote
#include "stdafx.h"
#include <stdio.h>
#include <iostream>
//defines allow us to create variables for permanent use
//its value cannot be changed
#define PI 3.14159

void firstTask() {	
	//create the three variables we need for the upcoming tasks
	//variables can only be defined once redefining will not be done
	int a, b;
	//std::endl breaks the line so that the next text starts in the next line
	std::cout << "THIS IS TASK 1" << std::endl;
	std::cout << "Please enter two integer numbers : " << std::endl;
	//read two values from the command line and put them into variables a and b
	std::cin >> a;
	std::cin >> b;
	//print out the values you just read in
	std::cout << "The numbers you entered are : " << a << " " << b << std::endl;
	//compute the sum and print out the result
	int sumab = a + b;
	std::cout << "The sum of those two number is : " << sumab << std::endl;
	//compute the difference and print out the result
	//remember redefining your variables is not allow so you cannot state int sumab = a-b at this point
	// you can either create a new variable or just write sumab = a - b
	int diffab = a - b;
	std::cout << "The difference between those two number is : " << diffab << std::endl;
	//compute the product and print out the result
	int prodab = a * b;
	std::cout << "The product of those two number is : " << prodab << std::endl;
	//compute the quotient and print out the result 
	//division by zero needs not to be cared about
	int quoab = a / b;
	std::cout << "The quotient of those two number is : " << quoab << std::endl;
	// Print an empty line to seperate from the next task
	std::cout << std::endl;
	//Finished First Task
}
void secondTask() {
	//create two variables with the names a and b
	int a, b;
	//print out some text to the command line
	std::cout << "THIS IS TASK 2" << std::endl;	
	std::cout << "Please enter two integer numbers : " << std::endl;
	//read two values from the command line and store them into a and b
	std::cin >> a;
	std::cin >> b;
	//compare if the first value read aka variable a to the second aka variable b
	//and then print out the corresponding messages accordingly
	if (a > b) {
		//if its the case then the following line is to be executed
		std::cout << a << " is bigger " << std::endl;
	}
	else if (a < b) {
		//if its the case then the following line is to be executed
		std::cout << b << " is bigger " << std::endl;
	}
	else {
		//if none of the before is the case then the following line is to be executed
		std::cout << "The numbers are equal" << std::endl;
	}
	//alternative solution
	/*int big;
	if (a > b) {
		big = a;
	}
	else if(a<b){
		big = b;
	}
	else {	
		std::cout << "The numbers are equal" << std::endl;
	}
	if(a != b)
		std::cout << big << " is bigger " << std::endl;	
	*/
	//prints and empty line
	std::cout << std::endl;
	//Finished Second Task
}
void thirdTask() {
	//create a variable with the name radius of type integer
	int radius;
	//print out some stuff to the commmand line
	std::cout << "THIS IS TASK 3" << std::endl;	
	std::cout << "Please enter the radius of your circle : " << std::endl;
	//we now read the value of the radius from your keyboard and store it into radius
	std::cin >> radius;
	//Then we compute the diameter of the circle 
	int diameter = 2 * radius;
	//and print it out
	std::cout << "The diameter of your circle is : " << diameter << std::endl;
	//Then we compute the circumference of the circle and print it out
	std::cout << "The circumference of your circle is : " << diameter * PI << std::endl;
	//Then we compute the area of the circle and print it out
	std::cout << "The area of your circle is : " << radius * radius * PI << std::endl;
	std::cout << std::endl;
	//Finished Third Task
}
void forthTask() {
	//dont forget to put #define PI 3.14159 before the function 
	//we need three variables now we call them a, b and c 
	//and they are all of the type integer
	int a, b, c;
	//print some stuff on the command line
	std::cout << "THIS IS TASK 4" << std::endl;
	//ask the user for 3 integer numbers and store them into a,b,c
	// if you want to type the first value in the same line remove the std:endl here
	std::cout << "Please enter three integer numbers : " << std::endl;
	std::cin >> a;
	std::cin >> b;
	std::cin >> c;
	//compute the sum 
	int sum = a + b + c;
	//now its time to find out which one is highest and which one is the lowest
	//poshi and poslow is only for a future task and not necessary here
	int poshi, poslow;
	//we will use two variables to store the values for the neccessary comparison process
	//those values are
	//highest which will store the highest value
	//and lowest which will store the lowest value 
	int highest, lowest;
	if (a > b) {
		//if a is bigger than b,
		//a is currently the highest number and b the lowest
		poshi = 1;
		poslow = 2;
		highest = a;
		lowest = b;
	}
	else{
		//if b is bigger than a,
		//b is currently the highest number and c the lowest
		//if a is equal to b,
		//a is currently the lowest number
		//while at the same time the lowest number is the highest number
		poshi = 2;
		poslow = 1;
		highest = b;
		lowest = a;
	}
	// if c is higher than the highest number from previous comparison 
	// the variable value of highest becomes the value of c
	if (c >= highest) {		
		highest = c;
		poshi = 3;
	}
	// if c is lower than the lowest number from previous comparison
	//the variable value of lowest becomes the value of c
	if (c <= lowest) {		
		lowest = c;
		poslow = 3;
	}
	//print out the results
	std::cout << "The sum of those three number is : " << sum << std::endl;
	std::cout << "The average of those three number is : " << sum / 3.0 << std::endl;
	std::cout << "The product of those three number is : " << a * b * c << std::endl;
	std::cout << "The lowest of those three number is : " << lowest << std::endl;
	std::cout << "The highest of those three number is : " << highest << std::endl;
	//Finished Forth Task
}
// there can only be one main function in each c++ project 
// dont put multiple main functions in your projects
//all functions to be used inside another function have to be defined before 
//either in the same file or inside a included header file
int main()
{	
	firstTask();
	secondTask();
	thirdTask();
	forthTask();
	while (1) {}
    return 0;
}